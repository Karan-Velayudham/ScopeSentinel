[pytest]
asyncio_mode = auto
testpaths = tests
addopts =
    --cov=.
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
    --cov-fail-under=70

[coverage:run]
omit =
    tests/*
    venv/*
    workspace/*
    */site-packages/*
